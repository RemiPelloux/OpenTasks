# ============================================
# OpenTasks Production Environment Variables
# 
# Copy this file to .env and fill in the values
# NEVER commit .env to version control
# ============================================

# ============================================
# REQUIRED - Application Settings
# ============================================

# Application URL (your domain)
CLIENT_URL=https://opentasks.yourdomain.com

# Database password (use a strong password)
# Generate with: openssl rand -base64 32
DB_PASSWORD=your_secure_database_password_here

# Session secret (use a strong random string)
# Generate with: openssl rand -hex 64
SESSION_SECRET=your_session_secret_here

# Encryption key for API keys (exactly 64 hex characters)
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=your_64_character_hex_encryption_key_here

# CSRF secret (use a strong random string)
# Generate with: openssl rand -hex 32
CSRF_SECRET=your_csrf_secret_here

# ============================================
# OPTIONAL - Slack Integration
# ============================================

# Slack webhook URL for notifications
# Get from: https://api.slack.com/messaging/webhooks
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/xxx/xxx/xxx

# ============================================
# OPTIONAL - Advanced Settings
# ============================================

# Node environment (don't change)
NODE_ENV=production

# Server host (don't change for Docker)
HOST=0.0.0.0

# Server port (internal, nginx handles external)
PORT=3000

# Database URL (auto-configured in docker-compose)
# DATABASE_URL=postgresql://opentasks:password@postgres:5432/opentasks

# Redis URL (auto-configured in docker-compose)
# REDIS_URL=redis://redis:6379

# ============================================
# SSL Configuration (Choose one method)
# ============================================

# Option 1: Let's Encrypt (recommended)
# CERTBOT_EMAIL=your-email@domain.com
# CERTBOT_DOMAIN=opentasks.yourdomain.com

# Option 2: Custom SSL certificates
# Place your certificates in ./nginx/ssl/
# - fullchain.pem
# - privkey.pem

# ============================================
# SECURITY NOTES
# ============================================
# 
# 1. Use strong, unique passwords for all secrets
# 2. Never commit this file to version control
# 3. Back up your encryption key securely
#    (losing it = losing access to stored API keys)
# 4. Regularly rotate secrets in production
# 5. Use a secrets manager in cloud environments
#
# ============================================


